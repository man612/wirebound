appId: com.wirebound.app
productName: Wirebound
asar: true
compression: maximum

directories:
  buildResources: build
  output: dist

extraResources:
  - from: bin/gnirehtet-rust-win64
    to: bin/gnirehtet-rust-win64
  - from: bin/platform-tools
    to: bin/platform-tools
  - from: src/renderer/src/assets/icon.png
    to: icon.png

files:
  - '!**/.vscode/*'
  - '!dist/**'
  - '!bin/**'
  - '!src/*'
  - '!scripts/**'
  - '!*.exe'
  - '!*.blockmap'
  - '!*.zip'
  - '!*.7z'
  - '!electron.vite.config.{js,ts,mjs,cjs}'
  - '!{.eslintcache,eslint.config.mjs,.prettierignore,.prettierrc.yaml,dev-app-update.yml,CHANGELOG.md,README.md}'
  - '!{.env,.env.*,.npmrc,pnpm-lock.yaml}'
  - '!{tsconfig.json,tsconfig.node.json,tsconfig.web.json}'

win:
  executableName: wirebound
  icon: build/icon.ico
  target:
    - nsis

nsis:
  artifactName: ${name}-${version}-setup.${ext}
  installerIcon: build/icon.ico
  uninstallerIcon: build/icon.ico
  shortcutName: ${productName}
  uninstallDisplayName: ${productName}
  createDesktopShortcut: always
